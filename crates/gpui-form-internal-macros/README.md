# gpui-form-internal-macros

Internal helper macros used by `gpui-form-core`.

This crate is not intended for direct use. It provides small derives that reduce
boilerplate in the core component definitions.
